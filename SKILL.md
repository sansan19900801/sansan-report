---
name: sansan-report
description: 把多次 sansan-save 存档合并成可交付的 Markdown 报告。用户要求汇总诊断、整理报告或生成可分享材料时使用。
---

# sansan-report：诊断报告

你的工作是：把 sansan-save 留下的多份存档合并成一份可读、可分享、可归档的诊断报告。

报告内容必须来自当前项目存档目录下的存档文件，按时间顺序合并、去重、分类，保证每条内容都能追溯到用户已经确认过的状态。

脚本路径记为 `$A=~/.agents/skills/sansan-report/scripts/archive.py`。存档根目录、项目名、文件排序与 sansan-save / sansan-restore 完全一致，收集存档和生成报告路径都用脚本完成，不要手写 glob。

---

## 用户面向的措辞约定

跟用户对话一律用中文，不暴露内部术语：「snapshot」→「存档」，「session」→「对话」，「slug」→「项目」。frontmatter 字段名和 sessions/slug 是技术标识，不出现在用户对话里。

---

## 为什么需要报告

诊断结论容易漂在聊天里。客户想发给合伙人、三周后回顾、跟外部顾问对账，都得自己截图复制。报告把累积存档固化成一份带日期、带索引、可追溯的 Markdown 文档。

---

## 触发方式

| 命令 | 行为 | 脚本 |
|---|---|---|
| `/sansan-report` | 当前项目全部存档合并成报告 | `$A collect --json` |
| `/sansan-report --since YYYY-MM-DD` | 只合并该日期之后 | `$A collect --since 日期 --json` |
| `/sansan-report --slug <项目>` | 指定项目 | `$A collect --slug 项目 --json` |
| 「出报告」「打包」「整理一份」「给合伙人看的」 | 等价于默认命令 | 同上 |

---

## 存档根目录

项目名、存档根目录解析规则与 sansan-save 相同，由 `archive.py` 统一实现（`.sansan/config.json`，default/project/custom 三模式）。配置损坏、模式不支持、根目录指向 `/`/家目录/项目根时脚本会报错，你如实转达，**不猜测其他位置、不静默退回默认位置**。本技能只读配置，不改配置；要改位置引导用 `/sansan-save location`。

---

## 工作流程

### Step 1：确认有数据可合并

运行 `$A collect [--slug 项目] [--since 日期] --json`，看 `count`：

- 0 份 →「`{项目名}` 下还没有可汇总的存档。先处理真实任务、形成结论并保存，再回来生成报告。」
- 1 份 →「`{项目名}` 下只有 1 份存档，单份不需要合并报告，直接看这份文件即可。还是要强制出报告吗？」用户确认要再继续。
- ≥2 份 → 直接进入合并。

### Step 2：读取并解析

`collect --json` 已按文件名时间正序返回每份存档的字段和正文（`body`）。每份含 frontmatter（timestamp/title/source_skill/status/next_skill）和六段正文。某份字段缺失时尽量用现有信息，不因此中断。

### Step 3：用脚本生成报告路径（永不覆盖）

```bash
$A report-path [--slug 项目] --json
```

脚本创建 `reports/{项目}/` 目录并返回带当前时间戳的报告路径 `{存档根}/reports/{项目}/{YYYYMMDD-HHMMSS}-{项目}.md`。**每次新生成一份，绝不覆盖**，方便对比不同时点的诊断快照。

### Step 4：写报告（六段结构）

```markdown
# {项目名} 诊断报告

**生成时间**：{现在本地时间 YYYY-MM-DD HH:MM}
**累积存档**：{N} 份（最早 {日期}，最新 {日期}）
**主要走过的 skill**：{所有 source_skill 去重后列出}
**生成工具**：sansan-report

---

## 一、用户主诉的演进

按时间顺序，每份存档主诉一行：
- `2026-04-15` · {主诉一句话} · 来自 {source_skill}

末尾用一段话点出「关注点是怎么变的」（如从卖什么→卖给谁→怎么获客）。**这是少数允许总结的地方**，只描述演进路径，不引申、不推测、不发挥。

## 二、已确认的结论

合并所有「已得出的结论」，语义相近的去重，按时间倒序（新的在前）：
- {结论原文} · 出自 {存档标题} · {日期}

一条结论若被后续存档推翻或修正，**两条都列**，新的在前，旧的在后并标注「（已被后续诊断修正）」。

## 三、已否决的方向

合并「用户已否决的方向」：
- {方向} —— 否决理由：{理由} · 出自 {存档标题} · {日期}

没有则写「（暂无）」。

## 四、当前未解决的问题

合并两类：
1. status 为 in-progress 存档的「待验证假设」；
2. 最早提出、后续从未处理的方向。
- {问题/假设原文} · 首次出现 {日期} · 当前状态：{进行中/待验证}

## 五、推荐下一步

汇总各存档「已确认的下一步」+ next_skill 字段，按优先级：①最新存档推荐的最优先；②反复出现还没走的；③已被新推荐替代的标注「已被后续推荐替代」。用**一段话**讲清下一步做什么、为什么、对应哪个 skill，不列点。

## 六、附录：存档索引

按时间正序列出全部存档：

| 日期 | 标题 | 状态 | source_skill | 文件 |
|---|---|---|---|---|
| 2026-04-15 | 卖什么没想清楚 | 进行中 | sansan-diagnosis | `{文件名}` |

状态翻中文：进行中 / 已结论 / 已放弃。

---

报告由 sansan-report 自动生成。原始存档见当前项目 sessions 目录。需要更新就再运行一次 `/sansan-report`。
```

### Step 5：回执

```
报告已生成：{存档根目录}/reports/{项目名}/{文件名}

合并了 {N} 份存档（{起始日期} → {结束日期}）。
```

用户要发公众号或群里时，再加一句：

> 想发公众号或群里，可以用 `sansan-wechat-html` 把这份 Markdown 转成微信后台粘贴版。

用户没有发布需求时不加。

---

## 关键原则

1. **不从对话凭空总结**。每条都能追溯到具体存档的具体字段，这是报告可信度来源；
2. **永不覆盖**。每次新文件、带时间戳；
3. **不发挥**。除「主诉演进」允许简短总结，其余直接搬运存档字段；
4. **只生成 Markdown**，不主动出 PDF/HTML。

---

## 边界情况

- 存档含敏感信息（真实收入、客户名字）→ 原样保留、不脱敏；要脱敏应在 sansan-save 阶段做；
- 多份存档结论冲突 → 都列出，新的在前并标注修正关系；
- sessions 目录之外手动放的文件 → 不读，只读当前配置对应目录；
- 当前位置为空但用户说以前存过 → 用 `$A resolve-root` 显示存档根，引导 `/sansan-save location` 检查，不跨目录搜私人文件；
- 存档跨年 → 报告头部明确写出时间跨度。

---

## 说话风格

1. 回执只一段：路径 + 数量 + 时间跨度；
2. 不解释报告里写了什么，用户自己会看；
3. 报告正文不加感叹号、表情、鼓励语，这是给客户看的产物。

---

## 语言

- 用户用中文就中文，用英文就英文；中文遵循《中文文案排版指北》；
- 报告语言与存档一致。

---

完成当前任务后直接结束，不主动追加与当前任务无关的引导。
